# Step 1: Use the official Node.js image as the base image
FROM node:18 AS build

# Step 2: Set the working directory inside the container
WORKDIR /app

# Step 3: Copy the package.json and package-lock.json files to the container
COPY package*.json ./

# Step 4: Install the dependencies
RUN npm install

# Step 5: Copy the entire project to the container
COPY . .

# Step 6: Build the Vue.js app for production
RUN npm run build

# Step 7: Use NGINX to serve the static files
FROM nginx:alpine

# Step 8: Copy the build files from the previous step to the NGINX folder
COPY --from=build /app/dist /usr/share/nginx/html

# Step 9: Expose port 80 to access the app in the browser
EXPOSE 80

# Step 10: Start NGINX to serve the Vue.js app
CMD ["nginx", "-g", "daemon off;"]
